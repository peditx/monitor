{
  "name": "peditx-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required, just deploying functions.'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
